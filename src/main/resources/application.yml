server:
  port: 8082

spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/webquiz
      username: admin
      password: admin
      driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

bezkoder:
  app:
    jwtSecret: "======================BezKoder=Spring==========================="
    jwtExpirationMs: 86400000
